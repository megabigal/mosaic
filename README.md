To use: Add the dataset, run it through the cropper, and then run it through the main photomosaic.py, make sure to include the dataset and picture name
uses k-means clustering to find the dominant colour of each section of the image, and then finds the image in the dataset with the most similar dominant colour
utilises caching to speed it up, the program is still slow tho
